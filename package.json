{
  "name": "portfolio-joeytran",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "gen": "MOCK_MODE=false node --env-file=.env pipeline/generate.mjs",
    "deploy": "astro build && gh-pages -d dist --dotfiles",
    "new": "node pipeline/new.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "4.2.4",
    "astro": "6.1.10",
    "tailwindcss": "4.2.4"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
